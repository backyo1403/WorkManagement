// The Bach Office design is hand-written CSS custom properties (see
// src/app/globals.css), not utility classes — autoprefixer is all we need.
module.exports = {
  plugins: {
    autoprefixer: {},
  },
};
