# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# database (local-first SQLite file)
/prisma/dev.db
/prisma/dev.db-journal

# env
.env
.env*.local
!.env.example

# NOTE: public/ is the deployed site and IS tracked. Bach Office lives at
# public/index.html with its model at public/assets/tobby/ — one copy, the same
# files locally and in production, so the two can never drift apart.

# misc
.DS_Store
*.pem
npm-debug.log*
.vercel
*.tsbuildinfo
next-env.d.ts
